# Структура проекта Frontend

## Обзор

Проект использует TypeScript для разработки и компилирует его в JavaScript для браузера.

## Папки

### 📁 `src/` - Исходный код (TypeScript)
**Назначение:** Здесь находится весь исходный код, который пишет разработчик.

- `src/auth/` - модуль аутентификации (логин/регистрация)
- `src/chat/` - модуль чата
- `src/shared/` - общие утилиты (API клиент, типы)

**Файлы:** `.ts` (TypeScript)

**Важно:** Эти файлы НЕ используются напрямую браузером. Они компилируются в JavaScript.

---

### 📁 `public/` - Готовые файлы для браузера
**Назначение:** Здесь находятся все файлы, которые отдает nginx браузеру.

- `public/*.html` - HTML страницы ✅ **РЕДАКТИРУЙТЕ**
- `public/css/` - стили CSS ✅ **РЕДАКТИРУЙТЕ**
- `public/js/` - **скомпилированный JavaScript** из `src/` ❌ **НЕ РЕДАКТИРУЙТЕ**

**Файлы:** `.html`, `.css`, `.js` (скомпилированные)

**Важно:** 
- TypeScript компилирует `src/**/*.ts` → `public/js/**/*.js`
- HTML и CSS - это **исходные файлы**, их можно редактировать напрямую
- JavaScript в `public/js/` - это **автоматически сгенерированные** файлы
- Все пути в HTML должны начинаться с `/public/...`

---

## Процесс разработки

```
1. Пишете код в src/**/*.ts
2. Запускаете: npm run build (или npm run watch)
3. TypeScript компилирует: src/auth/ui.ts → public/js/auth/ui.js
4. Браузер загружает: /public/js/auth/ui.js
```

## Команды

- `npm run build` - скомпилировать TypeScript один раз
- `npm run watch` - автоматическая компиляция при изменениях (рекомендуется)

## Почему нет дублирования?

**Раньше было:**
- `public/js/` - скомпилированные файлы
- `js/` - копия для доступа через `/js/...` (дубликат!)

**Сейчас:**
- `public/js/` - единственное место для JS файлов
- HTML ссылается на `/public/js/...` (без дублирования!)

## Структура файлов

```
frontend/
├── src/                    # Исходный код (TypeScript)
│   ├── auth/
│   ├── chat/
│   └── shared/
│
├── public/                 # Готовые файлы (отдает nginx)
│   ├── auth.html
│   ├── chat.html
│   ├── css/
│   └── js/                 # ← Скомпилированный JS из src/
│
├── tsconfig.json           # Настройки TypeScript
└── package.json            # Скрипты сборки
```

## Что можно и нельзя редактировать

### ✅ МОЖНО редактировать:
- **`src/**/*.ts`** - TypeScript исходники (ваш код)
- **`public/*.html`** - HTML страницы (исходные файлы)
- **`public/css/*.css`** - CSS стили (исходные файлы)

### ❌ НЕЛЬЗЯ редактировать:
- **`public/js/**/*.js`** - скомпилированный JavaScript (автоматически генерируется)

## Важно помнить

1. ✅ **Пишите TypeScript код в `src/`** - это исходники
2. ✅ **Редактируйте HTML в `public/*.html`** - это исходные файлы
3. ✅ **Редактируйте CSS в `public/css/`** - это исходные файлы
4. ❌ **НЕ редактируйте `public/js/`** - это сгенерированные файлы
5. ✅ **Всегда компилируйте TypeScript после изменений** - `npm run build` или `npm run watch`
6. ✅ **Используйте пути `/public/...`** в HTML и коде

